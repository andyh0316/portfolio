"use client";

import { ReduxProvider } from "@/store/providers/redux-provider";
import {
  Chakra_Petch,
  Jura,
  Kanit,
  Orbitron,
  Oxanium,
  Quantico,
  Sarpanch,
  Tektur,
  Tomorrow,
  Turret_Road,
} from "next/font/google";
import "./globals.css";
import { App } from "./app";

// export const metadata: Metadata = {
//   title: "Create Next App",
//   description: "Generated by create next app",
// };

let currentFont;

const jura = Jura({
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
  variable: "--font-jura",
});
currentFont = jura;

// ok to read
const tektur = Tektur({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800", "900"],
  display: "swap",
  variable: "--font-tektur",
});

// hard to read but very cool: use for headings
export const orbitron = Orbitron({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800", "900"], // Orbitron has all these weights
  display: "swap",
  variable: "--font-orbitron",
});

// ok to read
const quantico = Quantico({
  subsets: ["latin"],
  weight: ["400", "700"], // Quantico only has Regular (400) and Bold (700)
  display: "swap",
  variable: "--font-quantico",
});

// ok to read
const chakraPetch = Chakra_Petch({
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"], // Available weights for Chakra Petch
  display: "swap",
  variable: "--font-chakra-petch",
});

// ok to read
const tomorrow = Tomorrow({
  subsets: ["latin"],
  weight: ["100", "200", "300", "400", "500", "600", "700", "800", "900"], // All weights available
  display: "swap",
  variable: "--font-tomorrow",
});

// ok to read
const kanit = Kanit({
  subsets: ["latin"],
  weight: ["100", "200", "300", "400", "500", "600", "700", "800", "900"], // All weights available
  display: "swap",
  variable: "--font-kanit",
});

// ok to read
const oxanium = Oxanium({
  subsets: ["latin"],
  weight: ["200", "300", "400", "500", "600", "700", "800"], // Oxanium has weights from 200-800
  display: "swap",
  variable: "--font-oxanium",
});

// ugly
const turretRoad = Turret_Road({
  subsets: ["latin"],
  weight: ["200", "300", "400", "500", "700", "800"], // Available weights
  display: "swap",
  variable: "--font-turret-road",
});

// hard to read
const sarpanch = Sarpanch({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800", "900"], // Sarpanch available weights
  display: "swap",
  variable: "--font-sarpanch",
});

const globalFontFamily = `${currentFont?.style?.fontFamily}, Verdana, "Helvetica Neue", Arial, sans-serif`;

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <head>
        {/* <link
          href="https://fonts.googleapis.com/css?family=Jura:300,400,500,600,700&display=swap"
          rel="stylesheet"
        ></link> */}
      </head>

      <body
        style={{
          fontFamily: globalFontFamily,
          fontSize: "16px",
          margin: 0,
          padding: 0,
          boxSizing: "border-box",
        }}
      >
        <App>{children}</App>
      </body>
    </html>
  );
}
